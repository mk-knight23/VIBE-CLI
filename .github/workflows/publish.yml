name: Build, Publish & Release (vibe-cli)

on:
  push:
    tags:
      - 'vibe-cli-v*'          # Tag-triggered publish/release for CLI
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (vibe-cli-vX.Y.Z). If provided, will tag and publish.'
        required: false

permissions:
  contents: write
  packages: write

env:
  NODE_VERSION: 20.x

defaults:
  run:
    working-directory: vibe-cli

jobs:
  maybe-tag:
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.version != '' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create tag ${{ inputs.version }}
        run: |
          git fetch --tags
          if git rev-parse "${{ inputs.version }}" >/dev/null 2>&1; then
            echo "Tag already exists"; exit 0;
          fi
          git tag "${{ inputs.version }}"
          git push origin "${{ inputs.version }}"

  build-and-publish:
    needs: [maybe-tag]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - name: Derive version from tag
        id: version
        run: |
          REF="${GITHUB_REF##*/}"
          echo "ref=$REF" >> $GITHUB_OUTPUT
          echo "Version from tag: $REF"

      - name: Validate NPM_TOKEN secret
        run: |
          if [ -z "${{ secrets.NPM_TOKEN }}" ]; then
            echo "::error title=Missing Secret::NPM_TOKEN is not set; aborting publish."
            exit 1
          else
            echo "NPM_TOKEN present (length: ${#{{ secrets.NPM_TOKEN }}})"
          fi

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build

      - name: Pack tarball
        run: npm pack

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: vibe-cli-tarball
          path: vibe-cli-*.tgz

      - name: Publish to npm
        if: startsWith(steps.version.outputs.ref, 'vibe-cli-v')
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        if: startsWith(steps.version.outputs.ref, 'vibe-cli-v')
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.ref }}
          name: Release ${{ steps.version.outputs.ref }}
          body: |
            ## Vibe CLI ${{ steps.version.outputs.ref }}
            Automated release from tag push.

            ### Installation
            ```bash
            npm install -g vibe-cli
            ```

            ### Quick Start
            ```bash
            vibe --help
            ```
          files: |
            vibe-cli-*.tgz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        run: |
          echo "Release job complete. Ref: ${{ steps.version.outputs.ref }}"